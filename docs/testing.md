---
description: The zero-dependency node:test suite — layout, isolation patterns, security regressions, and the CDP-driven headless-Chrome tests that verify rendering and UI interaction.
tags: [testing, node-test, cdp, verification]
source:
  - scripts/test/**
  - scripts/lib/cdp.js
---

# Testing

Everything runs on `node:test` with zero dependencies:

```bash
node --test scripts/test/   # from the repo root — `npm test` is the same thing
```

778 tests at last count: ~190 drive a real browser (or headless printing) and skip when Chrome is absent, and three exercise the packed npm artifact end to end and skip without npm. `scripts/test/index.js` exists because `node --test <dir>` does not expand a directory on the pinned Node version — the directory resolves to `index.js`, which requires every `*.test.js` (see [gotchas/testing.md](gotchas/testing.md)).

## Suite layout

| File | Covers |
|---|---|
| `paths.test.js` | Root normalization, workspace keys, `insideRoot` traversal/symlink defense (including not-yet-existing targets). |
| `fsatomic.test.js` / `envfile.test.js` | Atomic writes, modes; parse-preserving env merge (comments, order, quoting, replace, dry-run). |
| `envcanvas.test.js` | The `.env`-as-native-form feature. `isEnvFile` (the one gate — `.env`/`.env.*`, nothing else), `parse` (ordered, dedup last-wins, unquote), and `merge`'s key **removal** (parse-preserving, CRLF-stays-CRLF). `virtualFormCanvasFor`: one `secret` field per key pre-filled, a missing file → an empty form, a non-env/directory → `null`, and the load-bearing security check — **every value is `registerSecret`-ed before the envelope returns**. Plus two real-Chrome (CDP) drives: pre-filled masked values → add a key → delete-after-confirmation writing back to disk; and masking-by-default + the additive copy-to-clipboard toggle (spied on `writeText`) + paste-adds-rows. Skips the browser drives without Chrome. |
| `redact.test.js` | Every redaction pattern plus registered exact values. |
| `registry.test.js` | Health-ping liveness, stale-entry cleanup, spawn-lock contention and stale-lock breaking. |
| `validate.test.js` / `catalog.test.js` | Every validator error code; per-kind chart rules; fieldset/ui/span rules; lean-vs-full catalog; the registry-tweak drift test. Plus **six that hold SKILL.md itself to the CLI**: every `document` key it shows must exist in the registry (now including the nested `cover.background` / `scrim` shape), every `catalog <name>` it offers must resolve, every flag it names must be one the CLI accepts, every **error code** it names must be one a surface actually emits, it must **teach the companion** (`enhances` — a feature an agent cannot use if never told it exists), and — the one a refactor needs — **it must not still teach the way that no longer exists**. That last guard fails the build if `.instantcanvas.json` or its `documents` map reappears in the agent's contract: an agent still reading about a deleted config would write a file nothing reads, see no error (there is no file to be wrong), and report success on a theme that never took. These are the only tests whose subject is the contract's *teaching* rather than its shape — the structural tests stay green while a sentence lies, which is exactly how the catalog's prose rotted invisibly. |
| `markdownsrc.test.js` | The markdown `src` extension allowlist and the kernel-side read guard (`src: ".env"` is never read), frontmatter stripping, and `data:`-URI image inlining with its confinement, size-cap, and fenced-code exclusions. Plus the **math pass** (`inlineMath`): the four delimiters (`$…$`/`\(…\)` inline, `$$…$$`/`\[…\]` display, display matched before inline), the price and escaped-dollar guards (`$5`, `\$10`), code-fence exclusion via the `blankCode` twin, and the error sentinel for bad LaTeX. Its sibling is `mdcanvas.test.js`: the same allowlist, guarding the other route. |
| `mathsvg.test.js` | The server-side LaTeX → inline-SVG renderer (`lib/mathsvg.js`, vendored MathJax). `render()` **strips the one `vertical-align` inline style** (so the emitted `<svg>` carries no `style=`), keeps `currentColor` + `ex` sizing, emits `<path>`-only glyphs with **zero shared ids / `<use>` / `<defs>`** (`fontCache:'none'`, so two formulas cannot collide), and returns `{ok:false}` with the parser message on invalid LaTeX rather than throwing. The no-style strip and the error-detect path were both sabotage-verified. |
| `mdcanvas.test.js` | The virtual canvas a markdown file *is*: the synthesised envelope (nothing written to disk), the H1-else-filename title, and the native-view degradation (raw HTML removed rather than escaped, HTML `<img>` → markdown image, remote image → placeholder, fenced examples untouched). Plus the security half — the virtual-canvas route refuses everything the markdown allowlist refuses (`.env`, `.txt`, traversal, symlink escape) — and the scan's `kind` / canvases-first ordering / separate `count` and `docCount`. |
| `provenance.test.js` | The `createdWith` stamp: only `stamp` writes it and only from `package.json`; idempotent, byte-preserving (pretty *and* minified), refuses non-canvas JSON; the re-serialize fallback when the marker is the last key; the nested marker decoy that cannot hijack the splice; drift validates cleanly and absence does not; the browser still renders an unstamped canvas; every example canvas is stamped and valid. |
| `theme.test.js` | The document color system, off the wire: `resolve()`'s layering (preset < token, a lone accent leading the colorway, **one palette color read as a lead the preset fills out while two or more ARE the colorway exactly as given**, junk dropped rather than passed to `setProperty`), the properties a preset is *chosen* for (`sepia` and `solarized` restyle the paper, the well-known palettes are carried faithfully), `presetList()` shipping **every** token rather than the two a chip renders, and the write boundary — `check()` refuses what `resolve()` would have quietly dropped, and **anything `check()` accepts also survives the canvas validator**, which is the property that keeps the browser from writing a canvas the CLI would then reject. Plus `jsonedit`'s splice: nothing but `document.theme` moves, an existing theme is replaced rather than merged, a decoy `"theme"` elsewhere in the file is not the one written, a minified file stays minified, and a canvas with no `document` object is refused so the caller can route elsewhere. |
| `companion.test.js` | The companion canvas — the envelope a markdown file never had. Binding is by **`enhances`, not by filename**: rename the companion to `anything.json` and nothing changes, while a report that merely *quotes* the README does not hijack it (declared, never sniffed). The sidebar shows **one** entry — the document, badged — and the companion is not counted as a canvas. The four validation rules, including `DUPLICATE_ENHANCES` naming *both* rivals and a companion that never reports itself as its own duplicate. Create-on-save: theming a bare `.md` writes a stamped, valid companion that renders its own document, and the `.md` is never touched. A **display** canvas gains a `document` spliced in — tabs kept, a minified block left minified — while a **form** canvas is refused, with the test *proving* the refusal is not merely cautious by showing the canvas would have become invalid. And the one that would have broken every README: a file **with a shields.io badge in it** can still have a cover — behind a companion its prose *degrades* (as `open README.md` does) instead of failing `REMOTE_ASSET_BLOCKED`, because a companion supplies an envelope, not an author. |
| `skillsconfig.test.js` | `skills-config.json`, the project's own committed config. **ABSENT ≠ CORRUPT**: a missing config is defaults, silently; a corrupt one **throws** (`CONFIG_UNREADABLE`) naming the file and the command that will find the defect, without quoting its bytes — and is **never repaired by deletion**, because it holds every skill's settings (a write against a broken file refuses rather than clobbering a neighbour's block). Plus the **key-order trap**, pinned against the *real* CLI: a deliberately non-alphabetical palette round-trips with its values byte-identical and its colorway in order, but its **keys alphabetised** — which is why the picker compares canonically, and why `JSON.stringify(a) === JSON.stringify(b)` would have silently un-lit the reader's own chip the first time they saved. And the drift test: the `config` schema shipped in `skill.json` must equal the one **generated from `lib/theme.js`**, because two hand-maintained validators will diverge. |
| `themecli.test.js` | The `theme` command — the agent's door to the color system, tested as the use case that forced it: branding a native `.md` in a brand scraped off a website. An agent asks what a document is wearing, saves the brand as a named palette, applies it — and the `.md` itself is never written, because the theme lands in a **companion canvas the command creates**, stamped and valid. **CLI/browser routing parity**: a canvas declaring `document` is spliced in place (byte-for-byte outside the theme, adopting the file's own tabs), a display canvas without one *gains* one (spliced, tabs kept, above `blocks`), a **slides canvas** routes into `presentation.theme` (created above `slides`, never a `document`), a **form canvas is refused** (`THEME_NEEDS_DOCUMENT`, nothing written — creating `document` there would make it fail validation), `--clear` on a canvas-declared theme is `THEME_DECLARED_IN_CANVAS` — the same answers `kernel.test.js` pins for `POST /api/theme`, which is the point of `lib/themestore.js` existing. Then the refusals that make the command worth running: `crimson` and `rgb(…)` are `INVALID_THEME` with **nothing written**, a preset typo lists the real names back, a palette shadowing a built-in is `PALETTE_NAME_TAKEN`. Plus `--list`, and `validate skills-config.json` — where an unparseable config is reported **without its own bytes**, and another skill's block is none of our business. |
| `gallery.test.js` | `lib/gallery.js` and the seventh block type: the recursive image listing (nested images found; dot-dirs / `node_modules` / symlinks skipped; a non-image never listed **and never opened**, proven with an `fs` read-hook; traversal refused; the cap + `truncated` flag; an empty dir valid), and the validator — `checkGallery`'s confinement and "is not a folder" `MISSING_SOURCE`, the layout/sort enums, and the `DOCUMENT_INTERACTIVE_BLOCK` / `PRESENTATION_INTERACTIVE_BLOCK` refusals (with the same canvas passing once the envelope is dropped). |
| `dir.test.js` | `lib/browse.js` `listDir` — the `/api/dir` listing, unit-tested without a kernel: items grouped canvases → env → documents → images (each A→Z), a `.env`/`.env.*` surfaced as its own `env` item (the one dotfile listed), companion collapse (the enhanced document badged, its companion dropped), the hidden-dir flag, `.git`/`node_modules` omission, dot-file omission, symlink refusal (a symlinked dir and a symlinked file), a `.env` *folder path* → `null` (byte-clean 404), the cap + `truncated`, and `dirsOnly`. Verified the symlink test fails when `lstat` is downgraded to `stat`. |
| `browsefilter.test.js` | The browse view's **Filter modal** + **Scope** control in real headless Chrome: a toolbar Filter button opens the modal; filtering to Images drops the canvas **and** the folders (Folders is its own type now, not auto-shown), and turning Folders on brings them back; Media subsumes and **locks** image/video/audio; the active filter shows as a ring + count badge; **All subfolders** flattens the subtree, drops folder tiles, and captions every tile with its folder; subtree + Images surfaces every image under here with its parent folder; a caption click routes to `#/f/<dir>`; zero inline styles. Plus the **performance half**, which is where the filter's disciplines are pinned: a tile computes `content-visibility: auto` on screen and `visible` under emulated print media; a folder-scope chip toggle hides tiles rather than rebuilding them, so the very same `<img>` node is still mounted after a round-trip **and the toggle inserts ZERO nodes** (a MutationObserver counts them — re-appending tiles "into place" is invisible churn that scales with the listing); a hidden **card** tile computes `display:none` and occupies no area (the specificity-sensitive case — `.gt` declares no `display`, so asserting on an image tile passes with or without the rule); the same hide holds in **list** mode; a filter change leaves the selection classed, counted and persisted; a subtree chip toggle **diffs** (a surviving tile keeps its node) and never prunes the union; and a filtered-out **video captures no poster until it is revealed**, counted as real resource timings. Skips without Chrome. |
| `reveal.test.js` | Folder reveal / open-in-terminal, in three halves. **Unit** (`lib/reveal.js`, no kernel): per-platform command and argv selection, observed through **shimmed executables on a temp `PATH`** that record their argv instead of launching anything — so `revealDir` on a folder literally named `a; touch pwned; echo x` must record it as **one argv entry** and leave no `pwned` behind (the proof that no shell is ever built); plus the headless-Linux heuristic returning false with nothing spawned, and the Linux terminal ladder returning false when every candidate is absent from `PATH` while `$TERMINAL` outranks the ladder when present. **Route** (a real spawned kernel, itself on a shimmed `PATH` so a 200 opens no window on the machine running the suite): a valid folder records exactly one invocation carrying the **absolute** path; an empty path means the root; outside-root is 403, and a **file**, a **symlinked directory** and a traversal are each a byte-clean 404 whose body carries none of the target; a bad action is 400, no token is 403, a non-JSON body is 415; and a kernel with an empty `PATH` under the fake-linux preload answers `{ok:false, code:'NO_FILE_MANAGER'}` **with a message** rather than a silent success. Every "nothing was spawned" assertion is paired with a **sentinel positive control** — a reveal known to work, fired afterwards, whose invocation must be the *only* one — because an empty log is otherwise satisfied by a route that never spawns at all. **Browser** (skips without Chrome): a sidebar row, a folder tile, the breadcrumb and the toolbar ⋮ all open the *same* four-item menu; every item is visible at rest; `.ic-menu [style]` is 0; the menu is clamped inside the viewport; Esc closes it without changing `location.hash`; `preventDefault` fires on folder anchors **and not elsewhere**; zero CSP violations and zero page errors. The `lstat` guard was sabotage-verified (downgrading it to `stat` reds the symlink test). |
| `upload.test.js` | Dropping files from the OS into the browse view, in three halves. **Unit** (`lib/upload.js`, no kernel): every `safeName` rejection — **both** separators (`path.basename` on POSIX does not treat `\` as one), `.`/`..`, a leading dot, the 255-**byte** boundary, each Windows reserved device bare and suffixed, a trailing dot or space, control characters — plus the double confinement (directory *and* joined destination), the `lstat`-not-`stat` refusal of a **symlinked directory** that resolves back inside the root, and `planUpload` reporting collisions with an `fs` read-hook proving it **opens nothing**. **Route** (a real spawned kernel): the round trip asserted by **hash**, not size, because a size agrees on two different files while the reader's data is silently mangled; a drop into a subfolder; the file carrying the **umask default rather than `fsatomic`'s `0o600`** (a dropped photo is the reader's own ordinary file, not state); 403 outside root, 400 for every bad name, byte-clean 404 for a file or symlinked directory as the target, 403 with no token, the 409 `needsConfirmation` shape, a successful `overwrite=1`, the 500-name batch cap, and 415 on the plan route for a non-JSON body. The **413 is exercised both ways** — a declared `Content-Length` over the cap, *and* a chunked body with no declared length at all, which only the byte counter can catch — with **no `.part` left behind** anywhere in the tree, and a just-under-the-cap upload as the positive control so the refusals cannot be satisfied by a route that never writes. **Browser** (skips without Chrome): a synthesized `DataTransfer` dropped on the pane. The missed-drop test asserts **`defaultPrevented`**, not `location.href` — a synthetic drop never navigates either way, so href alone could not fail. The in-place-sync test drops **renderable** files so a new tile must appear as its own positive control. **The empty space below the tiles and an empty folder are both proven to be in the zone**, by dropping at a *point* and asserting via `elementFromPoint` that the point was **outside** `.browse` (see [gotchas/frontend.md](gotchas/frontend.md)). Cancel is checked against a recursive before/after hash of the whole workspace, plus the folder refusal, zero inline styles, zero CSP violations. Four guards sabotage-verified: the `PUT` overwrite gate, `checkTarget`'s `lstat`, the document-wide `preventDefault`, and the drop zone's own geometry. Then **paste**, the same flow through the other gesture, driven as a synthesized `ClipboardEvent` + `DataTransfer` (headless Chrome blocks clipboard *read*, so a real system clipboard cannot be pasted from — `gotchas/testing.md`): a pasted file lands by **hash** and its tile appears without rebuilding the survivors; a colliding paste opens the *same* dialog and Cancel leaves a **recursive before/after** snapshot identical; a raw `image/png` with the generic clipboard name lands as `pasted-YYYYMMDD-HHMMSS.png` (asserted by hash, and the generic name is proven absent); an item overlay open means nothing is written; and plain text is a silent no-op — **no toast, no request**, paired with the file paste as its positive control, since a "no request" count is otherwise satisfied by a handler that never uploads at all. Two regressions guard what a document-level listener could break: the native `.env` form still turns `KEY=value` into rows while writing **no** file, and `⌘K`'s box keeps its own paste (asserted as `defaultPrevented === false` — a synthetic paste types into no input either way, so "the text arrived" could never fail). The empty-folder box is asserted as a **ratio** of the pane, never a pixel literal. Sabotage found the guards are **redundant**, which is itself the finding (`gotchas/testing.md`): the `.env` form sits inside the item overlay, so removing the overlay yield alone reds only the overlay test, and the env regression goes red only with the focus-in-field yield removed too. |
| `reroot.test.js` | Workspace **re-root** (the topbar breadcrumb), a real kernel spawned under a **fake `$HOME`** so the boundary is exercisable (`os.homedir()` honours `$HOME`). The `GET /api/workspace` crumb marks ancestors clickable up to home and no further; `POST /api/workspace/open` rejects a descendant (`NOT_ANCESTOR`), an above-home ancestor (`ABOVE_HOME`), a file (`NOT_DIR`), a missing path (`NOT_FOUND`) and an empty root (`BAD_ROOT`), and is token-gated; a **valid parent spawns/reuses a different kernel** (a new port) whose `/healthz` confirms it is rooted at the parent. |
| `reconnect.test.js` | The reconnect story. Kernel half: a respawn reuses the workspace **identity** (same port + token — the identity file survives a `kill -9` that the registry entry does not), and a taken port falls back to an ephemeral one keeping the token, identity rewritten. Browser half (skips without Chrome): killing the kernel under a live page turns the footer `disconnected` with a **visible** Reconnect call-to-action (asserted as computed display, never the attribute), the dialog names the exact `cd … && npx …@latest open .` command beside an always-visible copy button with zero inline styles — **wrapping** (`scrollWidth <= clientWidth`, a measurement) so a long path cannot hide its tail — and a respawned kernel makes the page **reload itself** back to `watching`, untouched. Then the stop pane: a faked drive-letter root pins the **two-line Windows form** in a real browser, the pane carries the same copy-ready command, and it too reloads itself when a kernel returns — even a QUICK restart, which never hits three misses (the stopped pane reloads on any kernel answer, or a fast restart would reconnect the WebSocket underneath it and strand it forever). Both core guards sabotage-verified (drop the identity write; drop the self-reload). |
| `imagemeta.test.js` | The zero-dependency dimension sniff — PNG, JPEG (past a large EXIF blob), GIF, WebP (VP8/VP8L/VP8X), BMP, best-effort SVG+ICO — from **buffers generated in-test** (no committed binaries): each format's dimensions, `null` on garbage / a truncated header / an empty file / AVIF-HEIC-TIFF, and a **bounded read** (a file far larger than the head cap returns without a full read). |
| `media.test.js` | The video/audio server half, over six real ffmpeg fixtures base64'd in `helpers/mediafixtures.js` (no committed binaries). Unit: `mediaKind` / `isRenderableMedia` / `isStreamableFile` / `galleryMime` over every extension (case-insensitive); the `parseByteRange` table; `mediaStat`'s kind + per-kind `renderable` and its **symlink refusal** (a `.mp4` symlinked at `.env` → `null`); `listDir` grouping images → videos → audios. Kernel HTTP: meta for video/audio (null dims) + a metadata-only `.mov`; the file route's full 200 + `Accept-Ranges` + byte equality; **206 slice equality** against the fixture; open-ended + suffix ranges; the 416 (carrying none of the file); a malformed Range → 200; a `.mov` → 404; the symlink → 404 on every surface; `?path=.env` byte-clean 404; `media-src 'self'` in the CSP; and a mixed image+video delete with a `.json` batch refused `NOT_A_MEDIA_FILE`. Range and the symlink guard were sabotage-verified. |
| `scan.test.js` | Marker discrimination, any-depth recursion (a nested folder is its own collection; an empty one is never listed), tree ordering; session lifecycle. (Markdown listing lives in `mdcanvas.test.js`.) |
| `kernel.test.js` | A real spawned kernel: healthz, 403s (token, Host), asset traversal, tree, WS round-trip, sessions, shutdown, and 404s for the removed reader-facing routes (browse / collection-delete — nothing listed, nothing deleted; `/api/workspace/open` is *no longer* among them — it was re-added, guarded, and is tested in `reroot.test.js`). Plus the markdown surface: a synthesised canvas served degraded (with the file on disk left untouched), a `.md` edit hot-reloading like a canvas edit, and the env surface — `GET /api/canvas?path=.env` now returns a **200 form** envelope (the sanctioned kernel-side reader — the value pre-fills the browser, which is the feature, not a leak), while a non-env, non-canvas file (`secrets.txt`) stays a byte-clean 404. Plus the theme routes: `/api/canvas` hands the page concrete hex; a `.md`'s theme **creates its companion**, which is then served under the *document's* path (`open` and `print` both get the cover for free) while the sidebar still shows one badged entry; a canvas that declares `document` is written **in place, byte-for-byte outside the theme**; a display canvas without one gains a `document`, while a **form canvas is refused** (`THEME_NEEDS_DOCUMENT`, 409); `/api/theme/plan` reports what a Save *would* do before it does it; a non-hex color is refused with nothing written **and no companion conjured to hold it**; a **slides canvas** is themed into `presentation.theme`, never a `document`; and a workspace keeps its own named palettes, served beside the built-in presets. Plus the **gallery routes**: the listing; the extension-gated `meta`/`file` surfaces (`?path=.env` is a byte-clean 404, a HEIC 200s on `meta` but 404s on `file`, correct Content-Type + `immutable` cache); a directory is a byte-clean 404 on `GET /api/canvas` and `open <folder>` broadcasts navigate `kind:"dir"` returning a `#/f/` url (the virtual gallery is retired); the `GET /api/dir` listing behind the browse view (dirs + grouped items with a `.env`/`.env.*` surfaced as its own `env` item, `.git`/`node_modules` omitted, dot-dirs `hidden`, token-gated, a `.env` passed as the *folder* path or a traversal → byte-clean 404); a hidden-folder edit reaches `broadcast` while `.git`/`node_modules` edits do not; and the guarded `POST /api/gallery/delete` — exact deletion with siblings surviving, atomic refusal on a mixed batch (a `.json`, a `.env`, a directory, a symlink, a traversal path), a directory never removed, partial-failure reporting, and the > 500-path cap. |
| `cli.test.js` | Usage/exit codes, unknown flags, validate/catalog output, the full open lifecycle including kill -9 recovery and `--result`; the Node ≥ 20 guard (a `-r` preload fakes an old runtime), an unwritable `--result`, and both browser-opener outcomes — shimmed `open`/`xdg-open` on PATH, and a fake-linux preload for the headless `BROWSER_OPEN_FAILED` warn. Plus `open` on a bare `.md` (no canvas written anywhere), the markdown refusals from `validate`/`stamp`, `open .env` now **accepted** (a synthesised form — `--timeout 1` yields a clean `timeout`, with no secret value in any channel), and the `assertReadable()` leak regression: `validate`/`stamp`/`print` never read a `.env`/`.txt`/`.yaml`, and none of its bytes reach stdout or stderr. Plus `open <folder>` opening the **browse view** (the result `url` ends `#/f/<rel>`) with nothing written to disk (asserted by a recursive directory snapshot), and the folder refusals from `validate`/`stamp`/`print`/`theme`. |
| `handshake.test.js` | The CLI against scriptable fake kernels (spawned `helpers/fakekernel.js`): version-skew restart, pending-session warn, a kernel that will not stop; kernel-rejected opens with and without error codes; session-poll tolerance (HTTP 500; three socket failures confirmed by a dead health ping; fifteen failures with a live kernel); the stop and spawn deadlines. |
| `forms.test.js` | Blocking `open` + HTTP submit: `.env` round-trip with redaction sweep, overwrite/outside-root 409 handshakes, confirm/timeout/cancel, json destinations, url-protocol and patternMessage rules. Plus the **native `.env` form** submit path: a value-changed overwrite naming only the changed key, a delete handshake naming the exact keys, an added key, an invalid key-name field error, and the sessionless direct save (`POST /api/env/save`) a human triggers — confined (a non-env path 404, a `.env` outside the root 403) and leak-swept. |
| `hardening.test.js` | Source scans (loopback literal, no third-party requires, timing-safe compare, no CORS, no `console.log` server-side) and runtime error codes (`WRITE_FAILED`, `SESSION_TIMEOUT`, `KERNEL_UNREACHABLE`). |
| `render.test.js` | Real headless Chrome via `helpers/cdp.js`: an adversarial canvas (splom + violin + 3D + skill-rendered kinds + a sweep + a markdown document) must draw every chart, expose a slider, highlight fenced code, inline a local PNG **and SVG** as `data:` URIs, typeset **math** as themed inline SVG (a deep-descent integral, a display sum, a matrix) whose glyph color follows the theme toggle while a `$5` price and a fenced `$x$` stay literal and bad LaTeX degrades to a `.math-error`, show an always-visible copy button **on screen** that survives a real clipboard round-trip (paper mounts none — see `document.test.js`), carry **no** `style=""` attribute, and log zero CSP violations. Skips without Chrome. |
| `document.test.js` | Document mode end to end. Contract: the `document` envelope shapes, interactive/sweep refusal, strict-hex colors, template-var warnings, the logo asset ladder; a spawned kernel inlining logos as `data:` URIs. Browser (skips without Chrome): brand tokens via CSSOM + the Plotly palette sink, the deck (cover/TOC/chapters/back cover) with TOC page numbers matching each anchor's sheet, the sheet-height invariant on every sheet, code-split reconstruction with highlighting intact, `printToPDF` `/Count` == sheet count for three fixtures, per-page `pdftotext` markers, the deck⇄continuous toggle reparenting the one live chart (universal: an undeclared canvas decks lazily with an auto-TOC, the reader-side TOC toggle repacks, an interactive canvas's muted deck button toasts the refusal), `beforeprint` relocation. Paper layout: fences **wrap** rather than overflow and a wide table **folds** rather than lose columns — both measured as `scrollWidth <= clientWidth`, never read back off the CSS, and the table is then checked against the PDF's own text layer (every one of an 11-column table's headers must reach paper; three of them used to be clipped away entirely, which no DOM assertion could see), headings carry real computed margins (the deck's `.md`-fragment reset once zeroed every one of them), and the deck holds zero copy buttons while the continuous view of the same canvas holds one per fence. |
| `galleryui.test.js` | The gallery UI in real headless Chrome: the grid mounts one tile per image with **no `style=""`**; sorting re-orders the same nodes (not a rebuild); the modal shows the real sniffed dimensions and zooms; arrows step and `Esc` restores focus; a file added on disk appears **without rebuilding the surviving tiles** (an expando on a surviving node stays intact — the in-place-sync proof); selecting leaves the clicked node `isConnected`; the confirm dialog counts exactly N and names each file; confirming deletes exactly those files from disk and their tiles while survivors stay; selection also enters by modifier-click and long-press; the deck toggle is muted with a toast naming the gallery; an authored gallery canvas hot-reloads on a layout flip; and zero CSP violations throughout. Skips without Chrome. The three guarded behaviours (re-render-on-select, the extension gate, the in-place sync) were sabotaged once each to confirm the matching assertion goes red (`gotchas/testing.md`). |
| `tree.test.js` | The sidebar **folders-only tree** in real headless Chrome: zero canvas/document leaf rows; `.git`/`node_modules` absent while a dot-folder is present and **un-muted** — the same computed opacity as a normal folder, asserted via `getComputedStyle` (the noisy dot-folders are already excluded, so a shown one is real); a chevron expands a subtree **without rebuilding sibling rows** (a held node stays `isConnected`); a folder-name click navigates to `#/f/` and highlights by a class toggle; a deep link auto-expands ancestors; zero page errors. Skips without Chrome. |
| `browse.test.js` | The **browse view** (`#/f/`) in real headless Chrome: the app lands on the root browse view (companion collapsed in the count, an enhanced document badged, a deck glyph); a **folder breadcrumb** leads the pane (a house at the root, a subfolder adding its own current segment, the house navigating back to root); **child folders render first, look distinct, and open on click** (`#/f/`); a folder renders its items grouped canvas → document → image → video → audio; sort direction flips **within** groups only; a file added on disk syncs **without rebuilding** the surviving tiles (`isConnected`); a document and an image tile both navigate to `#/c/`; **selection covers image/video/audio, never a canvas or document**; the grid/list toggle **stays** in select mode; a **long-press selects and stays selected on release**; zero inline styles and zero page errors. The drive is wrapped so a stray miss under load cannot poison the single-process suite. Skips without Chrome. |
| `mediaui.test.js` | The video/audio player + browse tiles in real headless Chrome (launched with the swiftshader defaults **plus** `--autoplay-policy=no-user-gesture-required`, since audio `play()` rejects without a gesture). One drive: the browse tile's `data:image/jpeg` poster + `0:01` badge + audio card + `1 video · 1 audio file` count + zero `.browse [style]`; the player mounting with a `0:01` duration and `64 × 48` dimensions, playing, and hiding/disabling the deck controls **with reasons**; the 2× speed menu **sticky across navigation**; keyboard seek / Space / Esc; audio playing and ending at 3× within ~1 s; the **error card** for a garbage file; **per-row copy** round-tripping the real clipboard for an image AND a video; the **dispose contract** (paused, `src`-less) on Esc and across a document↔video prev/next boundary; modifier-click selection + delete removing a video and a png from disk and grid; and **zero CSP violations** / page errors throughout. Removing `media-src` reds the CSP assertion, and dropping dispose's `removeAttribute('src')` reds the dispose assertions. Skips without Chrome. |
| `overlay.test.js` | The **item modal** (`#/c/`) in real headless Chrome: opening a document mounts the frosted modal (`#docModal`) over the folder's browse view, which **stays rendered behind** it; the relocated action cluster lives in the modal, the topbar island keeps only theme + stop, and the chrome carries **zero `style=""`**. The **backdrop never dismisses** (a stray scrim click leaves a form-bearing route intact); **Esc / ×** navigate to the owning folder's `#/f/`. The folder-path **breadcrumb** (no house — the × is the way back) navigates to a segment; sibling **prev/next** traverses the folder's items across all kinds and disables at the boundaries, crossing between a document and a neighbouring **image** and back. An image renders the shared zoom/pan stage with sniffed dimensions and disables the doc cluster with a reason; a metadata-only HEIC shows the placeholder card, never a broken `<img>`. The palette panel opens/closes from its new home, and **Esc is inert while focus is in a form field** so a session is never cancelled. Plus the **item info drawer**: the info button is the last control in the chrome and `aria-controls` the drawer; the drawer is collapsed by default (computed `display:none`, its own `[hidden]` rule beating the base), opens/closes via the button, its ×, and Esc **without navigating**, resets collapsed after prev/next, renders per-kind rows (a canvas's Path/Kind/Size/Created/Modified/Created-with, an image's Dimensions) with resting-visible copy buttons and zero inline styles, and computes a near-full-width sheet at ≤600px — while the media panel **moved** out of the stage (the stage holds no `.g-meta`, the drawer holds one) and a video's Duration value-syncs into the collapsed drawer. Each drawer assertion was sabotage-verified. Skips without Chrome. |
| `figures.test.js` | The **figure-number funnel**. `figureMap` numbers chart blocks 1..N in flat order (pages concatenated, sweeps counted, non-charts skipped, malformed → `[]` rather than throw); `blockIndex` is the flat index the deck writes as `data-chart`. A spawned kernel ships `figures` in the canvas payload (`[]` for a bare `.md`). Browser (skips without Chrome): the deck captions every chart `Figure N — <title>` (untitled → a bare `Figure N`) in flattened `pages[]` order, figures never enter the TOC, and the **D6 contrast is asserted in one DOM** — a declared document numbers its continuous view too, while an undeclared canvas's hidden continuous twin stays plain even as its deck numbers. Caption number == payload figure for every chart box. |
| `print.test.js` | The `print` command: result JSON shape, `/Count` == reported pages, the token/`127.0.0.1` leak regression on the PDF bytes, `CHROME_REQUIRED` on a bad or directory `CHROME_PATH` and when no Chrome is discoverable anywhere (a preload hides every install), canvas and `--out` workspace confinement, validation refusal before any Chrome, the non-document refusal, printing a bare `.md` with no canvas and no `document` object (sheets still equal PDF pages), a markdown file with **math** printed (2D SVG, so `/Count` stays exact and the PDF text layer carries no literal `$…$`/`\int` source), a **slides canvas** printed one landscape page per slide (`/Count` == slide count, a 960 × 540 page, notes excluded — read back through `pdftotext`), and the never-finished-rendering failure (`INSTANTCANVAS_PRINT_WAIT_MS`). Plus the **additive `figures[]`**: the dense fixture prints with per-figure facts (an `elided` count > 0), a page per chart whose caption `pdftotext` confirms is on that sheet, and the density warnings restated per figure; a quiet document reports `figures[]` with empty warnings; a markdown print carries an empty `figures[]`. Never asserts gl3d ink (a GPU-less machine prints blank 3D while everything else passes). |
| `snapshot.test.js` | The `snapshot` command. No browser needed: `--list` prints the figure map (companion-aware for a `.md`), `UNKNOWN_FIGURE` (including a non-integer) lists the valid map, a form is `SNAPSHOT_NEEDS_DECK`, an outside `--out-dir` is `PATH_OUTSIDE_WORKSPACE`, a bad `CHROME_PATH` and a hidden install are both `CHROME_REQUIRED`, a broken canvas is `INVALID_JSON`, an invalid one is refused before capture, a zero-chart canvas succeeds with `figures: []`. Capture (skips without Chrome): `--figure 2` writes exactly one PNG at a deterministic state-dir path with plausible A4 dimensions and its facts + warnings, a bare `--out-dir` run captures every figure there, and a default run leaves the workspace **byte-for-byte unchanged** (recursive before/after diff). Reads only a PNG's **signature and IHDR dimensions, never a pixel** (a gl3d chart blanks GPU-less). `runCli` is async so a capture never blocks the shared single-process event loop. |
| `presentation.test.js` | The presentation contract, off the wire. The **slides envelope is exclusive** — `slides` and `document` cannot co-exist (`DOCUMENT_ON_PRESENTATION`) — and each slide dispatches **per `layout`** to its required regions (a `quadrant` is **exactly four** cells, no more, no fewer), with the theme, footer and background rules a deck may declare. Any **interactive** block inside a deck is refused (`PRESENTATION_INTERACTIVE_BLOCK`) — a presentation is paper, not a form. `catalog presentation` and `catalog slide` resolve, and their examples validate. Plus the **Phase-B write path**: `jsonedit` splices `presentation.theme` **byte-identical outside it**, *creating* the `presentation` block above `slides` when absent; `themestore` routes a deck's theme there and **never conjures a `document`**; `--clear` on a canvas-declared theme is `THEME_DECLARED_IN_CANVAS`; and `planTheme` / `themeFor` report what a Save would land before it lands. |
| `slides.test.js` | The presentation in a real browser (skips without Chrome). The **filmstrip** renders one box per slide at the declared **16:9 geometry**, measured **computed, not read off the stylesheet**; a chart drew its `.main-svg`; the markup carries **zero CSP violations, zero injected `<style>`, and no `style=""`**. Backgrounds, footers and notes show as **browse chrome**, a crammed slide earns the **autofit badge**, and the **D9 topbar** swaps the deck toggle for **Present** while TOC/strips disable and the palette stays live. Then **PRESENTING**, driven by CDP **in-viewport** (never asserting `fullscreenElement` — §6.1): Present enters the stage, arrows/Space/click advance, ←/digits-then-Enter/Home/End navigate, **B** blanks, `/` stays inert, notes never reach the stage, and **Esc** returns — with the one **live chart node** moving onto the stage and back. |
| `mdview.test.js` | Real headless Chrome, focused on markdown **degradation** (the sidebar/browse now live in `tree.test.js`/`browse.test.js`): a README opened natively must render with its raw HTML *gone rather than escaped into view* — the failure no server-side assertion can see — its badge labeled, its local `<img>` inlined and decoded, its fenced `html` example verbatim, table alignment surviving as a class, GFM task lists rendering, zero inline styles, zero CSP violations, and the deck one click away. Skips without Chrome. |
| `e2e.test.js` | The packed npm artifact: the tarball allowlist (runtime ships, tests and workbench never), bin wiring and shebang, and the full agentic loop run by the installed bin from a scratch workspace. Skips without npm. |
| `crlf.test.js` | Line-ending preservation across **every** writer — the `.env` merge, the theme splice, the stamp. Each test asserts both directions (CRLF in → CRLF out with no lone LF, LF in → LF out), because a writer that churns a Windows file's endings rewrites every line of somebody's diff. Fails on the pre-fix writers, which emitted lone LF into CRLF files. |
| `selection.test.js` | `lib/selection.js` — the one read/write/clear path for the reader's persisted multi-selection, unit-tested without a kernel because it is a pure function of the filesystem plus the state dir. Sets the state dir with `||=` **before** requiring anything that reads it (the single-process suite shares one env; first loader wins — see [gotchas/testing.md](gotchas/testing.md)). |
| `rls.test.js` | `tools/rls.js`, the version bumper: keyword bumps with npm-style prerelease graduation, real semver precedence (a release outranks its own prerelease; build metadata never counts), invalid/equal/lower refusals that leave the manifest untouched, the leading-`v` hint, the nested-decoy splice guard, and formatting preservation. |
| `search.test.js` | Real headless Chrome: the search modal must open without fetching, match on name and folder, survive `c++` and `<script>` queries, never mark inside an entity, and wire ⌘K / `/` / arrows / Esc correctly. Skips without Chrome. |
| `palette.test.js` | Real headless Chrome, and every assertion in it is a bug that shipped. Picking a preset must **not close the panel** (the panel re-renders its own chip grid, which detaches the clicked node, so a bubble-phase outside-click test called every click inside it a click outside). A live color edit must **not replace the input the browser's picker is anchored to** — a native `<input type="color">` fires `input` on every pointer move, so rebuilding the grid killed the popup on the first click. Removing a colorway swatch must **stay** removed. Dark paper must flip the whole **sheet** — code syntax, card surfaces, chart template — and the mode must be derived from the **paper color**, not a flag, so a hand-darkened `paper` on a light preset flips it too. Plus: the two-screen editor (one screen, one Save), a saved palette matched by *value*, the paper-only controls disabling rather than vanishing while the palette stays live in either view, and the sticky deck⇄continuous choice surviving navigation *and* a canvas that has to refuse it. Skips without Chrome. |
| `topbar.test.js` | Real headless Chrome: the workspace path is a **breadcrumb** whose last segment is the current folder; the **copy icon** copies the full path while the breadcrumb itself does **not** (its clicks navigate/re-root), and the icon flashes then restores its own label (not the shared "Copy code"). And the **phone-only** sidebar "Move workspace" pencil — `display:none` on the desktop viewport, visible at 390 px — opens the re-root modal naming the current folder; a temp workspace is outside `$HOME`, so it offers no move-up targets (the floor holds) and the modal carries zero inline styles. The guarded re-root itself is `reroot.test.js`. Skips without Chrome. |

## Isolation patterns

- **State dir**: every test file that touches the registry sets `INSTANTCANVAS_STATE_DIR` with `||=` *before requiring* `lib/registry` — first loader wins, so the whole single-process suite shares one temp state dir instead of fighting over it (the plain-assignment version caused cross-file kernel misses).
- **Kernel tests are before-hook + top-level tests, never subtests** — on the pinned Node 24.0.x, sockets opened inside a `t.test()` subtest cannot reach servers created in the parent test's async context (see [gotchas/testing.md](gotchas/testing.md)).
- **Fake kernels are child processes** (`helpers/fakekernel.js`), never in-test `http` servers — on Node 24.0.x a server inside the runner process is invisible to subprocess clients (see [gotchas/testing.md](gotchas/testing.md)). They are created in `test.before` and killed in `test.after`, so a failing assertion cannot leak a live server that hangs the runner.
- Timing knobs for slow paths: `INSTANTCANVAS_LOCK_WAIT_MS` and `INSTANTCANVAS_SPAWN_WAIT_MS` make the two `KERNEL_UNREACHABLE` deadlines fast; `INSTANTCANVAS_PRINT_WAIT_MS` expires `print`'s render-readiness deadline. A size knob rides beside them: `INSTANTCANVAS_MAX_UPLOAD` shrinks the upload cap, so `upload.test.js` can drive the streaming byte counter past its limit without sending 2 GiB — a guard nobody can afford to exercise is a guard nobody has exercised.

## Coverage

```bash
npm run coverage        # full suite with a per-file report (node --experimental-test-coverage)
npm run coverage:cli    # the enforced gate: scripts/instantcanvas.js at 100 / 77 / 96 (lines / branches / functions)
```

The built-in reporter counts **subprocess** execution — every spawned CLI and kernel inherits coverage tracking — so the numbers are real even though almost every CLI test runs `instantcanvas.js` as a child process. Both commands double as hard release gates (see [releasing.md](releasing.md)).

`scripts/instantcanvas.js` holds **100% line coverage and 96.61% function coverage**, pinned by the `coverage:cli` thresholds so it cannot silently regress. The branch gate earns its keep: the companion work added a wave of defensive branches and pushed branch coverage from 77% down to 68%, and the gate refused the build until they were either **exercised** (the `--all` refusals, a palette deleted by name, a config that holds no block of ours, a `--clear` that must create nothing) or **deleted** as unreachable. That is the gate working — a threshold you lower to make a build pass is not a threshold. What the numbers deliberately exclude:

- One region carries a `node:coverage ignore` comment: the re-parse `catch` inside `spliceStamp`, defensively unreachable because the input already parsed as JSON — the reachable guard is the diff below it, which `provenance.test.js` exercises via the nested-decoy canvas.
- The uncovered branches are structural on a single platform/runtime: the `win32` side of the detached-spawn flag, the rethrow guards in `main().catch` and `cmdCatalog` (they fire only if error handling itself fails), health-object truthiness sub-branches that need a malformed registry entry, and the `.catch(() => false)` readiness-poll arrow in `print` (fires only if `evaluate` rejects mid-poll).

Reachability tricks (all `-r` preloads in `scripts/test/helpers/`): `fakenode18.js` fakes an old runtime for the Node ≥ 20 guard, `fakelinux.js` fakes display-less Linux for the browser warn, `nochrome.js` hides every Chrome install from discovery, and `nokernel.js` sabotages the kernel spawn for the spawn deadline.

One caveat pinned by experiment: `--test-coverage-include` silently drops files that never loaded, so a filtered gate would pass vacuously if the target file never executed. `coverage:cli` is safe because it runs the full suite, which always executes the CLI.

## Security regressions

`hardening.test.js` pins the security posture in source scans, so a regression fails before it ships: the server must bind the literal `127.0.0.1` (the wildcard address is asserted absent — the assertion builds the string dynamically so the test file passes its own scan), token comparison must use `crypto.timingSafeEqual`, no `Access-Control-Allow` headers, only `node:` builtins and relative requires anywhere, and the `SECRET_RETURN_BLOCKED` / `BROWSER_OPEN_FAILED` guards must exist. `forms.test.js` additionally greps every output channel for planted secret values.

## Browser verification

`render.test.js` **is in the suite**. It drives real headless Chrome through the zero-dependency DevTools-protocol client in `scripts/lib/cdp.js` (the repo's own masked-WebSocket knowledge, inverted: clients mask, servers don't — lifted out of the test helpers so the `print` command shares it; `test/helpers/cdp.js` is now a thin re-export whose DEFAULT launch flags remain the tests' swiftshader profile) — renders one deliberately adversarial canvas, and asserts that every chart box drew an SVG root with zero CSP violations. It exists because a chart can fail to draw with no error anywhere, and every server-side test still passes. It skips cleanly when Chrome is absent (`CHROME_PATH` overrides discovery).

Two traps it encodes:

- **Do not use `--dump-dom --virtual-time-budget`.** It needs no WebSocket client and is therefore very tempting, but virtual time runs the event loop to quiescence between steps, so races never manifest — it reported a full canvas on a build where a real browser dropped a chart.
- **Do not set a `Host` header on `/json/list`.** Chrome echoes it back when building `webSocketDebuggerUrl`; a portless `Host` yields a portless `ws://` URL that then connects to port 80.

`search.test.js` drives the same client one level deeper — it *clicks*, and asserts on what the click did. It exists because search has no server side at all: only a real browser can see it fail. It encodes the two traps in [gotchas/testing.md](gotchas/testing.md) — poll for `window.ic`, not for an element that exists before `app.js` binds it; and use a non-throwing `until()` so one dead step fails one assertion instead of sinking the hook. (The folder browser once taught the same lesson — `POST /api/browse` returned correct listings while the modal was unnavigable — before the feature was removed along with in-browser workspace switching.)

`document.test.js` uses the same client for the class of bug that only a laid-out page has: a fence that overflows its box on paper (`pre.scrollWidth <= pre.clientWidth` — a PDF has no scrollbar, so an overflowing fence is a *clipped* one) and a heading whose margins were silently zeroed (`getComputedStyle(el).marginTop`). Both are asserted as **computed values**, never by grepping the stylesheet: in each case the intended CSS rule was present and correct, and something more specific had beaten it (see [gotchas/testing.md](gotchas/testing.md)). The fixture had to be taught the hard case too — `fixtures/handbook.md` now carries a `<pre>` wider than the sheet, because the clip it exposes was previously *unfailable*, not merely untested.

**A chart's layout is geometry, so the test measures rectangles.** `render.test.js` mounts a bar chart of long account names beside a legend and asserts that **no tick label's bounding rect intersects the legend's** — the axis/legend collision exists nowhere else, because every layout number Plotly was handed looked correct while the pixels disagreed (see [gotchas/frontend.md](gotchas/frontend.md)). It fails against the pre-fix code with *6 of 8 tick labels overlapping*, which is the only reason to trust it passing. The same fixture pins the 30-character tick elision (two names over the cap come back elided, one under it comes back whole), and a **second chart pins the opposite direction**: an author's `options` patch on `layout.margin.b` / `layout.legend` must survive the auto-fit untouched, because `options` is applied last and is the author's final word. A fit pass that silently overwrote it would be the same class of bug from the other side.

**Tick density is asserted as px-per-label, never as a count** — a count reads fine at one pane width and lies at every other. `render.test.js` carries a 400-day line (which must come back as a `type: 'date'` axis carrying *no* forced tickvals) and a 60-category bar (which must keep its tick array, for the elided ticktext, while dropping the labels it has no room for), and asserts `axisPx / ticks >= 12` on both — the same threshold `AXIS_TOO_DENSE` quotes. A third chart pins the stand-down: an author's `options` patch on `layout.xaxis` survives untouched. Two heatmaps pin the label decision in **both** directions, because the old row-count gate was wrong in both: a 144-row grid with roomy cells must label every cell, and a grid whose cells are too short to hold a number must label none. All five go red against the pre-fix code (the date axis comes back `category`, the roomy grid comes back with zero labels), which is the only reason to trust them passing — and `print.test.js`'s own tick assertion had to be **rewritten**, because it asserted all 62 category ticks were rendered and was therefore pinning the bug in place (see [gotchas/testing.md](gotchas/testing.md)).

`document.test.js` pins the TOC's contents both ways, which is what makes the rule falsifiable rather than merely stated: a canvas **with** headings lists its headings and chapter names and **not** its chart titles (a caption is not a section), and a canvas with **no** headings at all lists its block titles (they are the only structure it has). Assert both halves or "drop the block titles" silently costs every prose-less deck its contents page.

`lib/cdp.js` hands `fn` a raw `send(method, params)` alongside `evaluate`, so a test — or a development-time script — reaches `Page.captureScreenshot` and `Emulation.setDeviceMetricsOverride` directly. The search modal was reviewed as rendered pixels in light and dark that way, not only as asserted DOM.

Real mouse and keyboard input (`Input.dispatchMouseEvent` — dragging a slider, hovering a chart) is still development practice rather than suite coverage. That practice caught the date-picker's self-closing arrows, the CSP-dropped grid styles, and the chart `options` series wipe-out.
