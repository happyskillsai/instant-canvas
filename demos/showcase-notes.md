### About this page
This markdown is loaded from **`demos/showcase-notes.md`** via the `src` variant — resolved server-side, never a raw file route. It supports lists:

- inline `code`, **bold**, *italics* and [links](https://example.com)
- blockquotes and tables below

> The kernel inlines me on every request, so editing the .md hot-reloads too.
