{
	"name": "instant-canvas",
	"version": "0.2.1",
	"description": "Local canvas runtime for coding agents: renders canvas JSON (markdown, KPIs, tables, 17 chart kinds, forms) via a per-workspace localhost kernel and collects input/secrets straight to local files.",
	"type": "skill",
	"license": "BSD-3-Clause",
	"repository": "https://github.com/nicolasdao/instant-canvas.git",
	"authors": [
		"Nicolas Dao <nicolas.dao@gmail.com>"
	],
	"keywords": [
		"canvas",
		"charts",
		"dashboard",
		"forms",
		"secrets",
		"env",
		"visualization",
		"localhost"
	],
	"dependencies": {},
	"systemDependencies": {
		"node": {
			"description": "Node.js >= 20 — runs the InstantCanvas CLI and per-workspace kernel (zero npm dependencies).",
			"check": "node --version",
			"install": {
				"darwin": "brew install node",
				"linux": "sudo apt-get install -y nodejs",
				"win32": "winget install OpenJS.NodeJS.LTS"
			}
		}
	}
}
